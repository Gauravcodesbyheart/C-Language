#include <stdio.h>
#include <string.h>
int main ()
{    
    char str [] = {"College Wallah is the best channel"};
    printf("%s\n",str);
    puts (str);  // %s is used to print the characters  // puts is also using to print the whole string
    return 0;
}