#include <stdio.h>
typedef float realnumbers;  // by doing so realnumbers can be used in place of float data type
int main ()
{
    int x;
    realnumbers y=3.14;
    printf("%0.2f",y);
    return 0;
}