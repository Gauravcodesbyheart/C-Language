#include <stdio.h>
int main ()
{
    int arr [5]= {1,2,3,4,5};
    for (int i=0; i<4; i++)
    {
        printf("%d ",arr[i]);  // using the foor loop is more ideal way to take input as well as output
    }
    return 0;
}