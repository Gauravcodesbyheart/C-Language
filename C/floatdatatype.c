#include <stdio.h>
int main ()
{
    float  x;
    float  y;
    x = 5;
    y = 2;
    float z = x/y;     // by using float keyword we cna see that we have an decimal no. in output 
    printf("%f",z);
    return 0;
}